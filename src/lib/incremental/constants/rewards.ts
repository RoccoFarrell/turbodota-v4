/**
 * Phase 7: PvE encounter rewards (gold, XP, post-win heal).
 */

/** Gold granted per encounter win. */
export const GOLD_PER_ENCOUNTER_WIN = 25;

/** XP granted per hero per encounter win. */
export const XP_PER_HERO_WIN = 10;

/** Post-win heal: fraction of max HP restored per hero (e.g. 0.1 = 10%). */
export const HEAL_PERCENT_ON_WIN = 0.1;
